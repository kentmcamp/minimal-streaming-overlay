<Window x:Class="minol.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:minol"
        mc:Ignorable="d"
        Title="Input and Time Overlay"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        >
    <Grid x:Name="RootGrid" Background="#1A000000">
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Edit" Click="Edit_Click"/>
                <MenuItem Header="Analyze Values" Click="AnalyzeValues_Click"/>
                <MenuItem Header="Countdown" Click="Countdown_Click"/>
                <Separator/>
                <MenuItem Header="Start" Click="Start_Click"/>
                <MenuItem Header="Pause" Click="Pause_Click"/>
                <MenuItem Header="Reset" Click="Reset_Click"/>
                <Separator/>
                <MenuItem Header="Exit App" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <Border Padding="12" CornerRadius="8">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical" Background="Transparent">
                <TextBlock x:Name="KeyText"
                           FontSize="28"
                           Foreground="#00FF00"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0"
                           Margin="0,0,0,4"/>

                <TextBlock x:Name="TimeText"
                           FontSize="48"
                           Foreground="#00FF00"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
